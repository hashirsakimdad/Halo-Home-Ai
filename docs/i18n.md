# Language (Urdu / English)

HoloHome responses should follow the user’s language (Urdu/English mixed).

Where this is enforced:

- System prompt template in `config/settings.py`
- Agent system prompts encourage bilingual support (especially `EducationAgent`)

