# Whisper Models

Whisper model is configured via:

- `.env` → `WHISPER_MODEL` (default `base`)

Common sizes: `tiny`, `base`, `small`, `medium`.

On low-power devices (Pi-class), start with `tiny` or `base`.

